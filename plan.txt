Big things:
--> Make a web-based frontend.

Little things:
--> Create a sensible way to automatically back up the hands database to JSON
to prevent data loss. 

There is a problem with the web-based frontned. Things are not running synchronously, the database gets a problem ahead of itself.



